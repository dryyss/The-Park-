/** Nombre max de photos communautaires par exemplaire (hors gradation). */
export const MAX_PHOTOS_PER_ITEM = 5;

/** Nombre max de photos par type (carte gradée / certificat). */
export const MAX_GRADED_PHOTOS_PER_KIND = 1;
